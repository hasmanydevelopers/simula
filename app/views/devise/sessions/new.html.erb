<div class="jumbotron" id="presentation">
    <h3>Bienvenido a la aplicación de control de sesiones de terapia simulada.</h3>
</div>
<div class="jumbotron" id="loginform">
    <h2>Iniciar Sesión</h2>

    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <div><%= f.label "Correo electrónico" %><br />
    <%= f.email_field :email, :autofocus => true %></div>

    <div><%= f.label "Contraseña" %><br />
    <%= f.password_field :password %></div>

    <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label "Recordarme" %></div>
    <% end -%>

    <div><%= f.submit "Ingresar", :class => 'btn btn-lg btn-success' %></div>
    <% end %>
    <div class="marg-sup-btn">
        <%= render "devise/shared/links" %>
    </div>
</div>
