<%= devise_error_messages! %>
<div class="jumbotron">
    <h2>Cambiar tu contraseña</h2>

    <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>

      <%= f.hidden_field :reset_password_token %>

      <div><%= f.label :password, "Nueva contraseña" %><br />
      <%= f.password_field :password, :autofocus => true %></div>

      <div><%= f.label :password_confirmation, "Confirmar la nueva contraseña" %><br />
      <%= f.password_field :password_confirmation %></div>

      <div class="marg-sup-btn"><%= f.submit "Cambiar mi contraseña", :class => 'btn btn-lg btn-success' %></div>
    <% end %>

    <div class="marg-sup-btn">
        <%= render "devise/shared/links" %>
    </div>
</div>
